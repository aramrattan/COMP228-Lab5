public class Calc {
	//change this method so that it outputs the required values
   
    //method to find factors
    public void factor(long numToFact, long factor)//numToFact is the MINTASKSIZE 
    {
    	//long rootOfNum = (long)Math.floor(Math.sqrt((double)numToFact));
    	
    		if((numToFact%factor)==0)
    		{
    			System.out.println(factor);
    		}
    		
    	
    	
    }
}
